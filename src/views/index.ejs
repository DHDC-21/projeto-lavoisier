<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Usuários</title>
</head>
<body>
  <h1>Usuários</h1>
  <ul>
    <% users.forEach(function(user) { %>
      <li><%= user.firstName %> <%= user.lastName %></li>
    <% }); %>
  </ul>
</body>
</html>
